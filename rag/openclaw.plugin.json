{
  "id": "rag",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "siliconflowApiKey": {
        "type": "string",
        "description": "SiliconFlow API Key for bge-m3 embedding (moegirl)"
      },
      "cohereApiKey": {
        "type": "string",
        "description": "Cohere API Key for embed-multilingual-v3 (wikipedia)"
      },
      "dataDir": {
        "type": "string",
        "description": "Data directory for logs (default: auto)"
      }
    }
  }
}
